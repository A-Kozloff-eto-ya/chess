# Roadmap — Развитие проекта Chess

> Фичи отсортированы по приоритету и зависимостям.
> Оценка трудозатрат: **S** (1-2 часа), **M** (3-5 часов), **L** (1-2 дня), **XL** (3-5 дней)

---

## Фаза 1: Стабилизация и базовые улучшения

### 1.1 Выбор контроля времени
- **Статус:** Реализовано
- **Описание:** Позволить игрокам выбирать time control перед игрой: Bullet (1+0, 2+1), Blitz (3+0, 3+2, 5+0, 5+3), Rapid (10+0, 15+10), Classical (30+0, 30+20)
- **Зависимости:** Исправление синхронизации таймера (2.5 в refactoring-plan)
- **Трудзатраты:** M
- **Замечания по качеству:**
  - ✅ REST-эндпоинт `move.post.ts` удалён — ходы обрабатываются только через WS, рассинхронизация устранена.
  - `clockSync` работает каждые 10 сек — допустимо, но возможен видимый drift.

### 1.2 rematch (Переигровка)
- **Статус:** Реализовано
- **Описание:** Кнопка "Rematch" после окончания игры. Отправка WS-сообщений `rematch_offer` / `rematch_accept` / `rematch_decline`. При accept — автоматическое создание новой игры с swapped цветами.
- **Трудзатраты:** M
- **Замечания по качеству:**
  - ✅ Server-side защита от двойного offer — трекается через `rematchOfferedBy` / `rematchAccepted` в GameRoom.
  - ✅ Accept отклоняется если нет pending offer или игрок пытается принять свой же offer.

### 1.3 Прерывание игры (Abort)
- **Статус:** Реализовано
- **Описание:** Возможность прервать игру до первого хода без потери рейтинга. После первого хода — только resign.
- **Трудзатраты:** S

### 1.4 Email-верификация
- **Статус:** Не реализовано
- **Описание:** Подтверждение email при регистрации. Отправка письма с токеном, верификация при клике.
- **Трудзатраты:** L

### 1.5 Сброс пароля
- **Статус:** Реализовано
- **Описание:** Форма "Forgot password", отправка email с токеном для сброса. Токен живёт 1 час, одноразовый.
- **Трудзатраты:** L
- **Замечания по качеству:**
  - ✅ Retry при ошибке отправки email (3 попытки с нарастающей задержкой). При полной неудаче токен удаляется из БД.
  - Анти-enumeration сохранён: `{ success: true }` всегда.
  - Одноразовый токен, expiry 1 час — всё корректно.

---

## Фаза 2: Социальные функции

### 2.1 Внутриигровой чат
- **Статус:** Реализовано
- **Описание:** Простой текстовый чат между игроками во время партии. WS-сообщения `chat`. История сообщений сохраняется в БД.
- **Трудзатраты:** M
- **Замечания по качеству:**
  - ✅ Таблица `chat_messages` создана (миграция `00006_chat_messages.sql`).
  - ✅ Сообщения сохраняются в БД при отправке, история подгружается при `join` (до 200 сообщений).
  - ✅ Базовая санитизация: обрезка до 500 символов, экранирование `<>`. Vue рендерит через `{{ }}` (авто-escape).
  - ✅ DOMPurify подключён на клиенте (defense-in-depth) — `ALLOWED_TAGS: []`, strip всего HTML.

### 2.2 Онлайн-статус пользователей
- **Статус:** Реализовано
- **Описание:** Показывать зелёный/серый индикатор рядом с username. Использовать `peerRegistry` для отслеживания подключённых пользователей. Отправлять `user_online` / `user_offline` через WS.
- **Трудзатраты:** M
- **Замечания по качеству:**
  - Реализация качественная: поддержка нескольких вкладок через `Set<WsPeer>`, корректные онлайн/оффлайн события только для друзей.
  - ✅ `lastSeenAt` timestamp сохраняется в БД при connect/disconnect. Offline-пользователи показывают "Last seen X ago" на странице друзей.
  - ⚠️ Real-time статус по-прежнему in-memory — при рестарте сервера `lastSeenAt` корректен, но WS-события теряются до повторного подключения (решается через Redis в 7.1).

### 2.3 Удаление аккаунта
- **Статус:** Не реализовано
- **Описание:** Кнопка "Delete account" в настройках профиля. Мягкое удаление (анонимизация данных) или полное. Обработка активных игр и друзей.
- **Трудзатраты:** M

### 2.4 Редактирование профиля
- **Статус:** Реализовано
- **Описание:** Добавить изменение username, avatar URL, био. API endpoint `PUT /api/users/me` расширен — username, bio, avatar.
- **Трудзатраты:** M
- **Замечания по качеству:**
  - ✅ Миграция для колонки `bio` добавлена (`00005_bio_column.sql`).
  - ✅ Server-side проверка уникальности username перед update — при коллизии возвращается 409.

---

## Фаза 3: Анализ и движок

### 3.1 Шкала оценки (Evaluation bar)
- **Статус:** Реализовано
- **Описание:** Визуальная полоса сбоку доски, показывающая оценку позиции. Обновляется после каждого хода. Использует Stockfish evaluation.
- **Трудзатраты:** M
- **Замечания по качеству:**
  - Хороший пул процессов Stockfish с очередью задач и таймаутами.
  - ⚠️ Эндпоинт `/api/engine/eval` не защищён авторизацией — любой может использовать анализ без логина (может быть намеренным решением).
  - Sigmoid-функция для визуальной шкалы корректна.

### 3.2 Анализ после игры (Post-game analysis)
- **Статус:** Типы `EngineEvaluation` и `AnalyzedMove` определены
- **Описание:** После окончания игры — автоматический анализ всех ходов. Показывать:
  - Лучший ход (Best move)
  - Ошибки/зевки (Blunder, Mistake, Inaccuracy)
  - Оценка позиции после каждого хода
  - Классификация ходов (!, ?, ??, ?!)
- **Трудзатраты:** XL

### 3.3 Multi-PV (несколько вариантов)
- **Статус:** Запланировано
- **Описание:** Показывать топ-3 линии Stockfish во время анализа. Отправлять `setoption name MultiPV value 3` движку.
- **Трудзатраты:** M

### 3.4 Подсказки (Hint arrows)
- **Статус:** Запланировано
- **Описание:** В режиме vs AI — кнопка "Hint", показывающая стрелку лучшего хода на доске. Использует `boardApi.drawArrow()`.
- **Трудзатраты:** S

### 3.5 Client-side Stockfish (WASM)
- **Статус:** Запланировано
- **Описание:** Загружать Stockfish WASM в браузере для мгновенного анализа без запросов к серверу. Экономит ресурсы сервера.
- **Трудзатраты:** L

---

## Фаза 4: Матчмейкинг

### 4.1 Быстрая игра (Quick Play / Matchmaking)
- **Статус:** Запланировано
- **Описание:** Система автоматического подбора соперника по рейтингу:
  1. Игрок выбирает time control и нажимает "Quick Play"
  2. Добавляется в очередь matchmaking
  3. Сервер подбирает соперника в диапазоне ±100 ELO (расширяется со временем)
  4. Автоматическое создание игры и уведомление обоих игроков
- **Компоненты:**
  - `server/utils/matchmaking.ts` — очередь и matching algorithm
  - WS-сообщения: `matchmaking:join`, `matchmaking:cancel`, `matchmaking:found`
  - UI: кнопка "Quick Play" на главной странице
- **Трудзатраты:** XL

### 4.2 Режим Spectator (Зритель)
- **Статус:** Типы определены, handler отсутствует
- **Описание:** Возможность наблюдать за чужой игрой в реальном времени:
  1. Ссылка для spectators (отдельный WS-канал или `game:watch`)
  2. Доска только для просмотра
  3. Счётчик зрителей
  4. Чат зрителей (опционально)
- **Трудзатраты:** L
- **Замечания:**
  - Определён тип `spectate` в `ClientMessage` и функция `spectateGame()` на клиенте.
  - ❌ На сервере в `_ws.ts` **нет обработчика** `case 'spectate'` — сообщение просто игнорируется.
  - Страница `game/[id].vue` — только просмотр завершённых игр через REST API, не live-трансляция.

---

## Фаза 5: Контент и экспорт

### 5.1 PGN Export / Import
- **Статус:** Не реализовано
- **Описание:**
  - **Export:** Кнопка "Download PGN" на странице завершённой игры. PGN уже генерируется через `generatePgn()` в `server/utils/chess.ts`.
  - **Import:** Загрузка PGN-файла для просмотра чужой партии. Парсинг через chess.js.
- **Трудзатраты:** M

### 5.2 FEN Export / Import
- **Статус:** Не реализовано
- **Описание:** Копирование текущей позиции как FEN. Установка произвольной позиции по FEN для анализа.
- **Трудзатраты:** S

### 5.3 Дебютная книга (Opening book)
- **Статус:** Не реализовано
- **Описание:** Показывать название дебюта (ECO code) во время игры. Интеграция с открытыми базами дебютов (например, ECO database JSON). Подсветка популярных продолжений.
- **Трудзатраты:** L

### 5.4 Турниры
- **Статус:** Не реализовано
- **Описание:** Создание и участие в турнирах:
  - Arena (швейцарская система)
  - Knockout (олимпийская система)
  - Round-robin
  - Настройки: time control, количество туров, макс. участников
  - Автоматический pairing и подсчёт очков
- **Трудзатраты:** XL (каждый формат)

---

## Фаза 6: UX и мобильная адаптация

### 6.1 Улучшенная мобильная версия
- **Статус:** Базовая адаптация через Tailwind
- **Описание:**
  - Адаптивная доска (занимает всю ширину экрана)
  - Сворачиваемая панель ходов (toggle)
  - Таймеры над/под доской
  - Touch-friendly элементы управления
  - Pre-move поддержка
- **Трудзатраты:** L
- **Что реализовано:**
  - ✅ Адаптивная доска (280–600px, динамический расчёт)
  - ✅ Tailwind responsive классы (`sm:`, `md:`, `lg:`)
  - ❌ Сворачиваемая панель ходов — нет
  - ❌ Touch-friendly контролы — мелкие кнопки
  - ❌ Pre-move — нет

### 6.2 Темы доски
- **Статус:** Не реализовано
- **Описание:** Выбор оформления доски и фигур:
  - Цветовые темы (классическая, синяя, зелёная, серая)
  - Наборы фигур (стандартный, миниатюрный, нестандартные)
  - Сохранение в профиле пользователя
- **Трудзатраты:** M

### 6.3 Звуковые эффекты
- **Статус:** Не реализовано
- **Описание:** Звуки хода, взятия, шаха, мата, ошибок. Вкл/выкл в настройках.
- **Трудзатраты:** S

### 6.4 Уведомления браузера (Push)
- **Статус:** Не реализовано
- **Описание:** Push-уведомления о:
  - Запросе в друзья
  - Ходе соперника (когда вкладка не активна)
  - Окончании времени
- **Трудзатраты:** M

---

## Фаза 7: Инфраструктура

### 7.1 Redis для production
- **Статус:** Запланировано (файлы отсутствуют)
- **Описание:** Использовать Redis для:
  - Хранения игровых комнат (вместо in-memory Map)
  - Pub/Sub для multi-instance WebSocket
  - Кэширование пользовательских сессий
  - Rate limiting
- **Трудзатраты:** XL
- **Замечания:**
  - Сейчас всё in-memory: gameRooms (`Map`), peerRegistry (`Map`), rateLimit (`Map`).
  - ⚠️ Rate limiter (`rateLimit.ts`) имеет тенденцию к утечке памяти — просроченные записи не удаляются активно, только при повторном обращении.
  - Нет ни одного Redis-файла в проекте.

### 7.2 Логирование и мониторинг
- **Статус:** Не реализовано
- **Описание:**
  - Структурированное логирование (Pino/Winston)
  - Логирование всех ходов и результатов игр
  - Метрики: активные игры, WS-соединения, время ответа API
  - Интеграция с Sentry или аналогом
- **Трудзатраты:** L

### 7.3 CI/CD Pipeline
- **Статус:** Не реализовано
- **Описание:**
  - GitHub Actions: lint, typecheck, test, build
  - Автоматический деплой на NuxtHub при merge в main
  - Preview deployments для PR
- **Трудзатраты:** M

### 7.4 Международная поддержка (i18n)
- **Статус:** Не реализовано
- **Описание:** Поддержка нескольких языков через `@nuxtjs/i18n`. Минимум: EN + RU.
- **Трудзатраты:** XL

---

## Критические баги и замечания по качеству

> Проблемы, найденные при аудите реализованных фич. Необходимо исправить перед дальнейшим развитием.

### BUG-1: ~~Отсутствует миграция для колонки `bio`~~ ✅ Исправлено
- **Приоритет:** Высокий
- **Файлы:** `server/db/schema.ts` (определяет `bio`), `server/db/migrations/00005_bio_column.sql` (добавляет `bio`)
- **Проблема:** Drizzle-схема определяла `bio: text().default('')`, но колонка не была создана в БД.
- **Решение:** Добавлена миграция `ALTER TABLE users ADD COLUMN IF NOT EXISTS bio TEXT NOT NULL DEFAULT '';`

### BUG-2: ~~Чат не сохраняется в БД~~ ✅ Исправлено
- **Приоритет:** Средний
- **Файлы:** `server/routes/_ws.ts` (handler `chat`), `server/db/schema.ts`, `server/db/migrations/00006_chat_messages.sql`
- **Проблема:** Сообщения чата пересылались real-time, но не персистились.
- **Решение:** Создана таблица `chat_messages`, сообщения сохраняются при пересылке, история (до 200 сообщений) подгружается при `join`.

### BUG-3: ~~REST `move.post.ts` не обновляет часы~~ ✅ Исправлено
- **Приоритет:** Средний
- **Файлы:** ~~`server/api/games/[id]/move.post.ts`~~ (удалён)
- **Проблема:** REST-эндпоинт хода не обновлял `whiteTime`/`blackTime`, в отличие от WS-обработчика.
- **Решение:** REST-эндпоинт удалён. Ходы обрабатываются только через WS, часы обновляются корректно.

### BUG-4: ~~Нет санитизации в чате~~ ✅ Частично исправлено
- **Приоритет:** Средний
- **Файлы:** `server/routes/_ws.ts` (handler `chat`)
- **Проблема:** Сообщения чата пересылались без какой-либо санитизации. Потенциальный XSS.
- **Решение:** Добавлена базовая серверная санитизация: обрезка до 500 символов, экранирование `<>`. На клиенте Vue рендерит через `{{ }}` (авто-escape). Полноценная библиотека (DOMPurify) не подключена.

### BUG-5: ~~Утечка памяти в rate limiter~~ ✅ Исправлено
- **Приоритет:** Низкий
- **Файлы:** `server/middleware/rateLimit.ts`
- **Проблема:** Записи в `Map` не удалялись активно — только при повторном обращении к ключу.
- **Решение:** Добавлен `setInterval` очистки просроченных записей каждые 60 секунд.

---

## Сводная таблица фич

| # | Фича | Приоритет | Трудозатраты | Зависимости | Статус |
|---|------|-----------|--------------|-------------|--------|
| 1.1 | Выбор time control | Высокий | M | Таймер-синхронизация | ✅ Реализовано |
| 1.2 | Rematch | Высокий | M | — | ✅ Реализовано |
| 1.3 | Abort игры | Высокий | S | — | ✅ Реализовано |
| 1.4 | Email-верификация | Средний | L | SMTP настроен | ❌ Не реализовано |
| 1.5 | Сброс пароля | Средний | L | SMTP настроен | ✅ Реализовано |
| 2.1 | Игровой чат | Средний | M | Единый WS | ✅ Реализовано |
| 2.2 | Онлайн-статус | Средний | M | — | ✅ Реализовано |
| 2.3 | Удаление аккаунта | Низкий | M | — | ❌ Не реализовано |
| 2.4 | Редактирование профиля | Средний | M | — | ✅ Реализовано |
| 3.1 | Evaluation bar | Высокий | M | Stockfish pool | ✅ Реализовано |
| 3.2 | Post-game анализ | Высокий | XL | Stockfish pool | ⏳ Типы определены |
| 3.3 | Multi-PV | Средний | M | Post-game анализ | ❌ Не реализовано |
| 3.4 | Hint arrows | Низкий | S | — | ❌ Не реализовано |
| 3.5 | Client-side Stockfish | Низкий | L | — | ❌ Не реализовано |
| 4.1 | Matchmaking | Высокий | XL | Рейтинг работает | ❌ Не реализовано |
| 4.2 | Spectator mode | Средний | L | WS рефакторинг | ⏳ Клиентский заглушка |
| 5.1 | PGN Export/Import | Средний | M | — | ⏳ Частично (PGN генерируется на сервере) |
| 5.2 | FEN Export/Import | Низкий | S | — | ❌ Не реализовано |
| 5.3 | Дебютная книга | Низкий | L | — | ❌ Не реализовано |
| 5.4 | Турниры | Низкий | XL | Matchmaking | ❌ Не реализовано |
| 6.1 | Мобильная версия | Высокий | L | — | ⏳ Базовая адаптация |
| 6.2 | Темы доски | Низкий | M | — | ❌ Не реализовано |
| 6.3 | Звуки | Низкий | S | — | ❌ Не реализовано |
| 6.4 | Push-уведомления | Низкий | M | — | ❌ Не реализовано |
| 7.1 | Redis | Высокий | XL | — | ❌ Не реализовано |
| 7.2 | Мониторинг | Средний | L | — | ❌ Не реализовано |
| 7.3 | CI/CD | Высокий | M | Тесты | ❌ Не реализовано |
| 7.4 | i18n | Низкий | XL | — | ❌ Не реализовано |

---

## Рекомендуемый порядок внедрения

```
Год 1, Квартал 1:
  └─ Фаза 1 (стабилизация) + Phases 2 (социальное) + исправление багов

Год 1, Квартал 2:
  └─ Фаза 3 (анализ) + Фаза 4.1 (matchmaking)

Год 1, Квартал 3:
  └─ Фаза 4.2 (spectators) + Фаза 5 (контент) + Фаза 6.1 (мобильная)

Год 1, Квартал 4:
  └─ Фаза 7 (инфраструктура) + Фаза 6 (UX)
```
