# Phase 1 — Чеклист проверки

## Автоматически проверено

- [x] `npm test` — 59 тестов проходят
- [x] `npm run build` — production-билд собирается без ошибок

---

## Ручная проверка (нужен npm run dev + PostgreSQL)

### 1. ELO рейтинг (BUG-6)

- [ ] Зайти под двумя пользователями, завершить партию
- [ ] Проверить в БД: рейтинг победителя вырос, проигравшего упал
- [ ] После 30+ игр у игрока рейтинг должен меняться медленнее (K=20 вместо K=40)

**Как проверить K-фактор:**
```sql
SELECT id, username, rating FROM users ORDER BY id;
```
Сыграть партию и проверить что рейтинг изменился на ожидаемое значение:
- Новичок (< 30 игр): ±20 за равную партию
- Опытный (≥ 30 игр): ±10 за равную партию

---

### 2. Draw exploit (BUG-7)

- [ ] Игрок A предлагает ничью (offer_draw)
- [ ] Игрок B нажимает "Принять ничью" → игра заканчивается ничьей ✓
- [ ] Новая игра: Игрок A нажимает "Принять ничью" БЕЗ offer → должен получить ошибку "No draw offer to accept"
- [ ] Игрок A предлагает ничью, делает ход → offer сбрасывается, принять нельзя

---

### 3. Move handler lock (BUG-8)

- [ ] Сыграть партию с нормальной скоростью — ходы обрабатываются корректно
- [ ] Нет "двойных ходов" при быстром перетаскивании фигур

---

### 4. Dockerfile (BUG-9)

- [ ] Запустить `docker build -t chess-test .` — билд завершается успешно
- [ ] В логах билда видно `npm run build` (не `npm run dev`)

---

### 5. .gitignore

- [ ] `git status` не показывает `shm_identity.json`
- [ ] `git status` не показывает `shm/`

---

### 6. .env.example

- [ ] В файле нет реальных ключей (Yandex поля пустые)

---

### 7. Abort/Resign авторизация (SEC-1/SEC-12)

- [ ] Игрок может сдаться в своей партии ✓
- [ ] Зритель/другой игрок НЕ может сдаться чужую партию (должен получить ошибку)
- [ ] Игрок может прервать свою партию до первого хода ✓
- [ ] Зритель НЕ может прервать чужую партию

---

### 8. Path traversal (SEC-8)

Проверить через curl/браузер:

```bash
# Должно вернуть 400
curl http://localhost:3000/api/users/avatar-serve/avatars/..%2Fetc%2Fpasswd
curl http://localhost:3000/api/users/avatar-serve/avatars/%00../etc/passwd

# Должно вернуть 400 (невалидный ключ)
curl http://localhost:3000/api/users/avatar-serve/invalid

# Должно вернуть аватар (если загружен)
curl http://localhost:3000/api/users/avatar-serve/avatars/123.png
```

---

### 9. Инкремент часов (BUG-10)

- [ ] Создать игру с time control 3+2 (3 мин + 2 сек инкремент)
- [ ] Сделать ход → проверить что время увеличилось на ~2 секунды
- [ ] То же самое для AI-игры с time control 5+3

**Как проверить визуально:**
1. Зайти на главную, создать игру 3+2
2. Подключиться двумя вкладками
3. Сделать ход → часы переключились, время ходившего должно было увеличиться
4. То же самое на `/play-ai?tc=3+2`

---

### 10. Rate limit на forgot-password (SEC-3)

```bash
# Отправить 4 запроса подряд
for i in {1..4}; do
  curl -X POST http://localhost:3000/api/auth/forgot-password \
    -H "Content-Type: application/json" \
    -d '{"email":"test@test.com"}'
  echo "---"
done
```

- [ ] Первые 3 — нормальный ответ
- [ ] 4-й — 429 Too Many Requests

---

# Phase 2 — Чеклист проверки (рефакторинг)

## Автоматически проверено

- [x] `npm test` — 59 тестов проходят
- [x] `npm run build` — production-билд собирается без ошибок

## Ручная проверка

### 11. Shared chess-ui утилиты

- [ ] Открыть `/import` — загрузить PGN, проверить что иконки фигур отображаются в списке ходов
- [ ] Открыть `/analyze/{id}` — проверить что quality badges (!!, !, !?, ?!, ?, ??) отображаются
- [ ] Открыть завершённую игру `/game/{id}` — проверить что MoveHistory показывает иконки фигур

### 12. Shared auth (useAuth)

- [ ] В хедере нажать "Выйти" → редирект на главную, тост "Вы вышли из системы"
- [ ] На мобильном: открыть drawer, нажать "Выйти" → drawer закрывается, редирект на главную

### 13. Shared game join (useGameJoin)

- [ ] На главной: ввести invite code, нажать Join → редирект на страницу игры
- [ ] В GameInviteModal: ввести invite code, нажать Join → модалка закрывается, редирект на игру
- [ ] Ввести неверный код → тост с ошибкой

### 14. Shared friend actions (useFriends)

- [ ] На странице друзей: отправить запрос → тост "Запрос отправлен"
- [ ] На странице друзей: удалить друга → тост "Удалён из друзей"
- [ ] На профиле: отправить запрос / удалить друга — тот же результат

### 15. SAN-to-UCI (server)

- [ ] `/api/engine/eval` с SAN ходами → возвращает корректную оценку
- [ ] `/api/engine/bestmove` с SAN ходами → возвращает корректный лучший ход

### 16. Email retry (server)

- [ ] Запросить сброс пароля → письмо приходит (если SMTP настроен)
- [ ] Запросить верификацию email → письмо приходит

### 17. Мёртвый код удалён

- [ ] `app/composables/useOrientation.ts` — файл не существует
- [ ] На главной нет закомментированных карточек Spectate/Tournament
